#include <iostream>
int main() {
	int k = 0, n = 0;
	double x, y;
	std::cin >> n;
	for (int i = 0; i < n; i++) {
		std::cin >> x >> y;
		if (x <= 2 && x >= -2) {
			if (y <= 1 && y >= -1) {
				
			}
		}
	}
}
